def test_dummy():
    # Arrange (optional): set up any data you need
    x = 1
    y = 2

    # Act (optional): call the function you want to test
    result = x + y

    # Assert: check the result
    assert result == 3
